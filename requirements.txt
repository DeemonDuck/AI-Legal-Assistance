# Core AI -- both providers; legal_ai.llm picks one via LLM_PROVIDER
groq>=0.11.0
anthropic>=0.40.0
pydantic>=2.9.0
python-dotenv>=1.0.0

# Document parsing
pypdf>=5.1.0
python-docx>=1.1.2

# UI
streamlit>=1.40.0

# No numpy. The corpus percentiles are hand-rolled in legal_ai/corpus.py so the
# arithmetic is visible in review, and at n<100 there is nothing to gain from a
# vectorised implementation. numpy was listed here for a while after the code
# stopped importing it, which cost every cold deploy the download for nothing.
